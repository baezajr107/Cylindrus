<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="example.project.cylindris" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainMenu"
            android:label="@string/title_activity_main_menu"
            android:screenOrientation="landscape" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScoreMenu"
            android:label="@string/title_activity_settings_menu"
            android:parentActivityName=".MainMenu"
            android:screenOrientation="landscape" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="example.project.cylindris.MainMenu" />
        </activity>
        <activity
            android:name=".DifficultyMenu"
            android:label="@string/title_activity_difficulty_menu"
            android:parentActivityName=".MainMenu"
            android:screenOrientation="landscape" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="example.project.cylindris.MainMenu" />
        </activity>
        <activity
            android:name=".testTetris"
            android:label="@string/title_activity_test_tetris"
            android:parentActivityName=".DifficultyMenu" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="example.project.cylindris.DifficultyMenu" />
        </activity>
    </application>

</manifest>
